# Глава 19: Выпадающие Списки (ComboBox) и Модели Данных (ListStore) в GTK

  

В этой главе мы углублённо изучаем работу с интерактивными элементами выбора в GTK: **`GtkComboBox`** (выпадающие списки) и **`GtkListStore`** (модели данных). Эти компоненты критически важны для создания динамических графических интерфейсов, где требуется выбор значения из предопределённого списка.

  

Мы рассмотрим два ключевых подхода к созданию выпадающих списков, охватывающих как простые, так и более сложные сценарии использования.

  

## Что мы изучим

  

* Как создать базовое окно с помощью `GtkWindow` и `GtkBox`.

* Как использовать **`GtkComboBoxText`** для создания простых выпадающих списков из текстовых строк.

* Как обрабатывать выбор элемента в `GtkComboBox` с помощью сигнала `"changed"`.

* Как использовать **`GtkListStore`** для построения гибких моделей данных с несколькими столбцами.

* Как связать `GtkComboBox` с моделью `GtkListStore` и отобразить содержимое с помощью **`GtkCellRendererText`**.

  

---

  

## 🔹 Ключевые функции, которые мы используем

  

| Функция | Назначение |

| :-------------------------------- | :----------------------------------------------------- |

| `gtk_window_new()` | Создаёт новое окно верхнего уровня |

| `gtk_window_set_title()` | Устанавливает заголовок окна |

| `gtk_box_new()` | Создаёт контейнер для размещения виджетов по одной оси |

| `gtk_container_add()` | Добавляет виджет в контейнер |

| `gtk_box_pack_start()` | Размещает виджет в `GtkBox` с заданными параметрами |

| `gtk_combo_box_text_new()` | Создаёт виджет `ComboBox` для простых текстовых списков|

| `gtk_combo_box_text_append_text()`| Добавляет текстовый элемент в `GtkComboBoxText` |

| `gtk_combo_box_text_get_active_text()` | Получает текст выбранного элемента из `GtkComboBoxText`|

| `gtk_combo_box_new_with_model()` | Создаёт `ComboBox`, связанный с моделью данных |

| `gtk_combo_box_set_active()` | Устанавливает активный (выбранный) элемент по индексу |

| `gtk_combo_box_get_model()` | Получает модель данных, связанную с `ComboBox` |

| `gtk_combo_box_get_active_iter()` | Получает итератор на выбранный элемент в модели |

| `gtk_list_store_new()` | Создаёт новую модель данных `ListStore` с заданными колонками и типами |

| `gtk_list_store_append()` | Добавляет новую пустую строку в `GtkListStore` |

| `gtk_list_store_set()` | Устанавливает значения ячеек в строке `ListStore` |

| `gtk_tree_model_get()` | Извлекает значения из модели данных по итератору |

| `gtk_cell_renderer_text_new()` | Создаёт объект для отрисовки текста внутри ячеек |

| `gtk_cell_layout_pack_start()` | Добавляет `GtkCellRenderer` в компоновку ячеек `ComboBox` |

| `gtk_cell_layout_set_attributes()`| Связывает свойство рендерера со столбцом модели |

| `g_signal_connect()` | Подключает callback-функцию к сигналу виджета |

| `gtk_widget_show_all()` | Отображает виджет и все его дочерние элементы |

| `gtk_main()` | Запускает главный цикл обработки событий GTK |

| `gtk_main_quit()` | Останавливает главный цикл GTK |

| `g_free()` | Освобождает память, выделенную GLib |

| `g_object_unref()` | Уменьшает счётчик ссылок на объект |

  

---

  

## 📁 Исходные файлы в этой главе

  

* `combo_simple_example.c` — Пример использования простого `GtkComboBoxText` со строками.

* `combo_liststore_example.c` — Пример использования `GtkComboBox` с `GtkListStore` и `GtkCellRendererText`.

  

---

  

## ✅ `combo_simple_example.c` — Простой текстовый список

  

Этот пример создаёт окно с выпадающим списком, содержащим названия языков программирования. При выборе нового элемента, его название будет выведено в консоль.

  

### 🔧 Сборка и запуск

  

Чтобы скомпилировать и запустить программу, выполните следующие команды в терминале:

  

```bash

gcc combo_simple_example.c -o combo_simple_example `pkg-config --cflags --libs gtk+-3.0`

./combo_simple_example

  

```

  

### 💻 Как устроено окно

  

-  **Окно** (`GtkWindow`) создаётся как контейнер верхнего уровня.

- Внутри окна располагается **вертикальный контейнер** (`GtkBox`) с отступами.

- В контейнер добавляется **`GtkComboBoxText`** — упрощённый виджет для работы с текстовыми строками.

- Элементы (`"C"`, `"Python"`, и т.д.) добавляются с помощью `gtk_combo_box_text_append_text()`.

- Изначально выбранный элемент устанавливается функцией `gtk_combo_box_set_active()`.

- Используется `g_signal_connect` для подключения функции `on_combo_changed` к сигналу `"changed"`, которая выводит выбранный текст в консоль.

  

### 🖥 Ожидаемый результат

  

При запуске появится окно с заголовком "Пример GtkComboBoxText" и выпадающим списком.

  

По умолчанию будет выбрано "C".

  

При выборе любого другого пункта из списка (например, **"Python"**), в терминал будет выведено сообщение:

  

```

Вы выбрали: Python

  

```

  

----------

  

## 🚀 `combo_liststore_example.c` — Список с моделью данных `GtkListStore`

  

В этом примере создаётся `GtkComboBox`, связанный с гибкой моделью данных `GtkListStore`. Каждый элемент в списке имеет не только отображаемый текст, но и ассоциированный числовой идентификатор (ID), который также извлекается при выборе.

  

### 🔧 Сборка и запуск

  

Чтобы скомпилировать и запустить программу, выполните следующие команды в терминале:

  

Bash

  

```

gcc combo_liststore_example.c -o combo_liststore_example `pkg-config --cflags --libs gtk+-3.0`

./combo_liststore_example

  

```

  

### 💻 Как устроено окно

  

- Создаётся **модель данных `GtkListStore`**, которая определяет две колонки: для строки (`G_TYPE_STRING`) и для целого числа (`G_TYPE_INT`).

- Данные (`"Опция А"`, `101`; `"Опция Б"`, `102` и т.д.) добавляются в `GtkListStore` построчно с использованием `gtk_list_store_append()` и `gtk_list_store_set()`.

- Затем создаётся **`GtkComboBox`**, который связывается с этой моделью данных с помощью `gtk_combo_box_new_with_model()`.

- Для отображения текстовых данных из модели используется **`GtkCellRendererText`**. Он привязывается к первой колонке модели (`COLUMN_NAME`) через `gtk_cell_layout_set_attributes()`.

- Используется `g_signal_connect` для обработки выбора элемента. Функция `on_combo_liststore_changed` извлекает из модели не только текст, но и соответствующий ID выбранного элемента.

  

### 🖥 Ожидаемый результат

  

При запуске появится окно с заголовком "Пример ComboBox с ListStore" и выпадающим списком.

  

По умолчанию будет выбрана "Опция Б".

  

При выборе любого пункта из списка (например, **"Опция В"**), в терминал будет выведено сообщение, включающее как название опции, так и её ID:

  

```

Вы выбрали: Опция В (ID: 103)

  

```

  

----------

  

## 📚 Дополнительные ресурсы

  

- [GtkComboBox - Официальная документация GTK3](https://docs.gtk.org/gtk3/class.ComboBox.html)

- [GtkComboBoxText - Официальная документация GTK3](https://docs.gtk.org/gtk3/class.ComboBoxText.html)

- [GtkListStore - Официальная документация GTK3](https://docs.gtk.org/gtk3/class.ListStore.html)

- [GtkCellRendererText - Официальная документация GTK3](https://docs.gtk.org/gtk3/class.CellRendererText.html)

- [GtkTreeModel - Официальная документация GTK3](https://docs.gtk.org/gtk3/iface.TreeModel.html)

  

----------

  

## 🌐 Руководство

  

- [Справочник «C & GUI на Raspberry Pi Zero 2 W» на GitHub](https://github.com/AIDevelopersMonster/C_GUI_Handbook)

  

----------